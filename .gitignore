node_modules/
dist/

*.debug.json
*.ndjson
debug.ndjson
pi-vcc-om-cooldown.json
*-pending.json
*-pending.stale.json

# Archived working docs stay local; real docs are committed
docs/archived_docs/

# Reproducible analysis artifacts (regenerated by scripts/)
tmp/

# All skills are local-only except lockstep (which is committed)
.pi/skills/*
!.pi/skills/lockstep/

# Subagent reports are local-only
.pi/subagents/

# Windows NTFS alternate data stream artifacts
*:Zone.Identifier

.agent/